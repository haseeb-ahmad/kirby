.modal
  %header
    = link_to '#', data: {dismiss: "modal"} do
      = icon('cross')
    %h3=t 'kirby.photos.new_folder'

  = form_for [kirby, :admin, @media_folder] do |f|
    %section
      = f.text_field :name, placeholder: Kirby::MediaFolder.human_attribute_name(:name)
    %footer
      = link_to t('kirby.cancel'), '#', data: {dismiss: 'modal'}
      = button_tag type: :submit, class: 'primary' do
        %i.icon.icon-plus{style: 'margin-right: 4px'}
        =t 'kirby.photos.new_folder'
