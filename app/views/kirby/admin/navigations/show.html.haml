- content_for(:header_actions) do
  = link_to t('kirby.edit', label: @navigation.label), kirby.edit_admin_navigation_path(@navigation), class: 'button'

.well
  .dd#pages_list{data: {:"sort-url" => kirby.sort_admin_navigation_url(@navigation) }}
    %ol.dd-list
      = render partial: 'navigation_item_nested_list', collection: @navigation.navigation_items.roots
